import React from 'react'

const Reactrouting = () => {
  return (
    <div>
        {/* these is just basic routing */}
{/* if here we used anchor  tag instead of Link then the page would reload on every navigation so we use Link to avoid reloading  */}
  {/* <nav className="flex justify-center gap-5 mb-5" aria-label="Main navigation">
    <Link to="/">Home</Link>
    <Link to="/about">About</Link>
    <Link to="/contact">Contact</Link>
  </nav>

  <Routes>
    <Route path="/" element={<Home />} />
    <Route path="/about" element={<About />} />
    <Route path="/contact" element={<Contact />} />
  </Routes> */}
    </div>
  )
}

export default Reactrouting